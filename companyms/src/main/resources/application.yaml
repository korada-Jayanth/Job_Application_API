spring:
  application:
    name: companyms
  datasource:
    url: jdbc:mysql://localhost:3306/companyms?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Hasinee@2010
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update  # validate for prod
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true  # Optional: H2 fallback only
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
